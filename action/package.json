{
  "name": "@gitscore/action",
  "private": true,
  "version": "0.1.0",
  "description": "GitScore PR Comment action proof-of-concept. Run via npm install && npx @vercel/ncc build index.js -o dist to bundle.",
  "main": "index.js",
  "scripts": {
    "bundle": "npx @vercel/ncc build index.js -o dist"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "node-fetch": "^3.3.2"
  }
}